title('scenario')
# Name the output fields, comma list, like: 'dx,dy'
# Provide a list of values for the fields, like: [ [1,2], [1,3], [0,1] ]
# Pick "which" entry from the list
# The values appear in the outputs
# Warning: Currently hard-coded to force "float" outputs.

input('fields', str)
input('sets',list)
input('which',int)

field_list = fields.split(',')

for i,f in enumerate(field_list):
    output(f,sets[which][i]+0.0)
